#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n,sum=0;
    cin >> n;
    /* this is so funny 
    n = pow(5, n);
    int i = 2;
    while (i--)
    {
        int r = n % 10;

        sum = (sum * 10) + r;
        n /= 10;
    }
    for (int i = 0; i < 2; i++)
    {
        cout << sum % 10;
        sum /= 10;
    }
    */
   
    cout<<25<<endl;
}